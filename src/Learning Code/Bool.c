#include <stdbool.h>
#include <stdio.h>

int main() {
    bool flag = true;

    if (flag) {
        printf("flag is TRUE\n");
    }
    else {
        printf("flag is FALSE\n");
    }

    return 0;
}
